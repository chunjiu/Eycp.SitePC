<table width="100%" cellpadding="0" cellspacing="0" class="tb_listOne">
    <tr>
        <th>统计</th>
        {{each winTotal win}}
        <th>{{win.headerName}}</th>
        {{/each}}
    </tr>
    <tr>
        <td>命中概率</td>
        {{each winTotal win}}
        <td>{{win.percent}}</td>
        {{/each}}
    </tr>
</table>